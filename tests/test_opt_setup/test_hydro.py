
def test_hydro_feasible():
    """
    GIVEN number of scenarios `N` and discount factor `lam`
    WHEN create hydro lp via `opt_setup_hydro_basic`
    THEN returned LP must be solvable
    """
    pass